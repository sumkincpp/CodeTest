line <- "fsdfsdfsd"

length(line) # incorrect -> 1
nchar(line)  # strlen, correct

line <- "fsdfsdfsd"

res <- grep('i+', line, TRUE)

integer(0)
logical(0)


3. вывод print непонятные скобочки [1] [0]

4. в редакторе нет и не работает Shift + Tab